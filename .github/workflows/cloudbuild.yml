name: Deploy to Google Cloud Hosting on merge
'on':
  push:
    branches:
      - main

steps:
  - name: 'gcr.io/cloud-builders/mvn'
    args: ['install']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '--tag=gcr.io/gleb-abramov/app', '.']
images: ['gcr.io/gleb-abramov/app']